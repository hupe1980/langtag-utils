{
  "name": "@wapps/langtag-utils",
  "description": "A collection of useful utilities for bcp47 and rfc4647",
  "version": "1.0.0",
  "author": "hello@webappsolutions.de",
  "repository": "hupe1980/langtag-utils",
  "keywords": [
    "bcp47",
    "rfc4647",
    "langtag",
    "subtag",
    "language",
    "lng",
    "lang"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest --env=jsdom",
    "test:watch": "jest --watch --env=jsdom",
    "build": "rimraf lib && babel src --out-dir lib --ignore **/__tests__",
    "build:watch": "rimraf lib && babel -w src --out-dir lib --ignore **/__tests__",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^8.2.6",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.3.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-prettier": "^2.6.2",
    "jest": "^23.4.2",
    "prettier": "^1.14.2",
    "rimraf": "^2.6.2"
  }
}
